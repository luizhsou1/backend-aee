version: "3"

services:
  postgres:
    image: postgres:12-alpine
    ports:
      - "5433:5432"
    container_name: "postgres-special"
    restart: always
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: special
      POSTGRES_PASSWORD: 123

volumes:
  pgdata:
